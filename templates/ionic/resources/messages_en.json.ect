{
  <% include 'resources/messages-common_en.json', @mergeEnv({models: @models}) %>,
  <% include 'resources/messages-page_en.json', @mergeEnv({models: @models}) %>,
  <% include 'resources/messages-model_en.json', @mergeEnv({models: @models}) %>
}
