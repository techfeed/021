angular
  .module('app')
  .controller('LoginController', ['$scope', 'Person', function($scope, Person) {
  }])
  .controller('SignupController', ['$scope', 'Person', function($scope, Person) {
  }]);
